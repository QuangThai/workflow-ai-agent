Run finalization hygiene checks before `/kd-handoff-dev`.

Load the `kd-ship` skill and follow its workflow.

1. Validate QA Full Gate and review gate status
2. Validate rollback/config/migration completion notes completeness
3. Append Ship Readiness Report
4. Block and mark ticket if critical readiness checks fail

Ticket to validate: $ARGUMENTS
