Check workflow environment readiness.

Load the `kd-health` skill and follow its workflow.

1. Validate required folders/files
2. Validate core stage skill and command availability
3. Validate utility command availability
4. Return a structured health report with missing dependencies

Optional focus area: $ARGUMENTS
